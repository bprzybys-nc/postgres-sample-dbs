## [Unreleased]
- Decommissioned the `periodic_table` database and removed all associated configuration, monitoring, and SQL files as part of the DB decommission workflow. No references to `periodic_table` remain in the repository.
